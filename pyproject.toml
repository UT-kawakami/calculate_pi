[project]
name = "calculate-pi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.3.4",
    "pytest>=9.0.0",
]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
